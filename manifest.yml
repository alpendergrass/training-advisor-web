---
declared-services:
  mean-mongo:
    label: mongodb
    plan: 100
applications:
- name: training-advisor
  host: training-advisor
  memory: 256M
  services:
  - training-advisor-mongodb
  env:
    NODE_ENV: cloud-foundry
