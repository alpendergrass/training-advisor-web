---
declared-services:
  mean-mongo:
    label: mongodb
    plan: 100
applications:
- name: training-advisor
  host: training-advisor
  memory: 2G
  services:
  - training-advisor-mongo
  env:
    NODE_ENV: cloud-foundry